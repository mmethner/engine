#
# this is an example config file
#

core:
  #will be set later
  url:
  # values: prod|test
  environment: test
  # Just leave as is if namespace is not important
  namespace: \Engine

locale: 
  default: en
  available: [en,de]

database:
  enable: false
